// RETO 29 — configuración pública.
// MODO 'demo': todo vive en este navegador, no hace falta backend (para probar).
// Para el despliegue real, quita MODO (o ponlo a "real") y no hace falta tocar nada más:
// el frontend habla con /api/reto29 en el mismo dominio. API_BASE solo hace falta si la
// función vive en otro dominio.
window.RETO29_CONFIG = {
  MODO: "demo",
  API_BASE: "",
};
